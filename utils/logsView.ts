// import path from "path";
// import fs from "fs"
// import { getFormattedDate } from ".";
// const filepath=path.join(process.cwd(),"logs")
// type logtype = "error" | "info" ;

// export const loggerInformation=(type:logtype)=>{
// const readFIle=fs.readFileSync(path.join(filepath,`${type || "error"}-${getFormattedDate()}.log`)).toString().split("\n").filter((val)=>val!=="")
// return readFIle
// }
